Open your webbrowser type : localhost
1. localhost (wampserver must be working)
2. check localhost/phpmyadmin (must be working also)
3. load the jobIT(updated).sql in your workbench
4. Put the temp-login.html and createAccount.php in the c:/wamp/www directory
5. now type localhost/temp-login.html
6. Input sample data, then check db if it works. :)